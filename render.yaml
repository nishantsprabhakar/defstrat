services:
  - type: web
    name: defstrat-dashboard
    env: node
    plan: free
    buildCommand: ""
    startCommand: "node server.mjs"
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
